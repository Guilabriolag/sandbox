export const Geometry={distanceToSegment(p,a,b){const dx=b.x-a.x,dy=b.y-a.y,t=Math.max(0,Math.min(1,((p.x-a.x)*dx+(p.y-a.y)*dy)/(dx*dx+dy*dy)));return Math.hypot(p.x-(a.x+t*dx),p.y-(a.y+t*dy))},pointInPolygon(p,pts){let inside=false;for(let i=0,j=pts.length-1;i<pts.length;j=i++){const a=pts[i],b=pts[j];if(((a.y>p.y)!=(b.y>p.y))&&(p.x<(b.x-a.x)*(p.y-a.y)/(b.y-a.y)+a.x))inside=!inside}return inside},hit(e,p){const g=e.geometry;if(g.type==="rect")return p.x>=g.x&&p.x<=g.x+g.width&&p.y>=g.y&&p.y<=g.y+g.depth;if(g.type==="point")return Math.hypot(p.x-g.x,p.y-g.y)<14;if(g.type==="line")return g.points.some((a,i)=>i&&Geometry.distanceToSegment(p,g.points[i-1],a)<10);return g.type==="polygon"&&g.points.length>2&&Geometry.pointInPolygon(p,g.points)}};